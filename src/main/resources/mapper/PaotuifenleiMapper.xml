<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.paotui.mapper.PaotuifenleiMapper">
    <resultMap id="BaseResultMap" type="com.paotui.entity.Paotuifenlei">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, name, create_time
    </sql>

    <select id="selectByPrimaryKey" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/> from paotuifenlei where id = #{id}
    </select>

    <select id="selectAll" resultMap="BaseResultMap" parameterType="java.util.Map">
        select <include refid="Base_Column_List"/> from paotuifenlei
        <where>
            <if test="name != null and name != ''">and name like concat('%',#{name},'%')</if>
        </where>
        order by id desc
    </select>

    <delete id="deleteByPrimaryKey">
        delete from paotuifenlei where id = #{id}
    </delete>

    <insert id="insert" parameterType="com.paotui.entity.Paotuifenlei" useGeneratedKeys="true" keyProperty="id">
        insert into paotuifenlei (name, create_time) values (#{name}, now())
    </insert>

    <insert id="insertSelective" parameterType="com.paotui.entity.Paotuifenlei" useGeneratedKeys="true" keyProperty="id">
        insert into paotuifenlei (name, create_time) values (#{name}, now())
    </insert>

    <update id="updateByPrimaryKeySelective" parameterType="com.paotui.entity.Paotuifenlei">
        update paotuifenlei
        <set>
            <if test="name != null">name = #{name},</if>
        </set>
        where id = #{id}
    </update>

    <update id="updateByPrimaryKey" parameterType="com.paotui.entity.Paotuifenlei">
        update paotuifenlei set name = #{name} where id = #{id}
    </update>
</mapper>
